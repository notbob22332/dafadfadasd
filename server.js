# Fernbrook Food

Upload the CONTENTS of this ZIP directly to the root of your GitHub repository.

For Render use a Web Service:
- Build: npm install
- Start: npm start
- No Publish Directory

Set these environment variables:
DISCORD_WEBHOOK_URL
SMTP_HOST
SMTP_PORT
SMTP_USER
SMTP_PASS
SMTP_FROM

The webhook stays server-side.
